# Ensure Windows batch/cmd files always use CRLF — CMD.EXE label lookup breaks on LF-only files
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# Shell scripts use LF
*.sh    text eol=lf

# Everything else: normalise on commit, checkout per OS
*       text=auto
